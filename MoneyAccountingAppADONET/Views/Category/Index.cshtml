@model IEnumerable<MoneyAccountingAppADONET.Models.Category>

<h2>Category</h2>

<p>
    <a class="btn btn-primary" asp-controller="Category" asp-action="Create">Add New Category</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>
                    <form asp-action="Delete" method="post" asp-controller="Category">
                        <input type="hidden" name="id" value="@item.Id" />
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
